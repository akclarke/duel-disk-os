// @testing-library/jest-dom is not installed in this project — the game logic
// tests don't assert on DOM nodes. Re-add the import here if it's ever needed:
// import '@testing-library/jest-dom/extend-expect';
